export const metadata = {
  title: "Profile",
  description: "Manage your ARNADA POOL identity, track your matches, and view your ranking.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
