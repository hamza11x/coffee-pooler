export const metadata = {
  title: "Login",
  description: "Sign in to your ARNADA POOL account to track your stats and join tournaments.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
