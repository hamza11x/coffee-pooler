export const metadata = {
  title: "Register",
  description: "Join the elite at ARNADA POOL. Create an account to start your billiards legacy.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
